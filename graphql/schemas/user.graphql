type User {
  id: ID!
  name: String!
  email: String!
  image: String!
  password: String
  wallet: Int!
  token: String
}

type VerifyEmailToken {
  email: String!
  expiredDate: String!
}

input UpdateInputData {
  name: String
  newPassword: String
}
